{
  "name": "@nest-admin/auth",
  "version": "0.0.1",
  "main": "dist/index.js",
  "author": "Danila Osin <danilaosin12@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prebuild": "rm -rf ./dist",
    "build": "tsc && yarn resolve-paths",
    "resolve-paths": "resolve-tspaths",
    "prestart": "yarn build",
    "prerepl": "yarn build",
    "lint": "prettier --config .prettierrc --write ./src",
    "ci": "yarn install --frozen-lockfile"
  },
  "dependencies": {
    "@nestjs/common": "^9.0.5",
    "@nestjs/core": "^9.0.5",
    "@nestjs/typeorm": "^9.0.0",
    "debug": "^4.3.4",
    "reflect-metadata": "^0.1.13",
    "resolve-tspaths": "^0.7.4",
    "rxjs": "^7.5.6",
    "typeorm": "^0.3.7"
  }
}
